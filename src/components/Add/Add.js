const Add = (props) => {

    return (
        
        <div>

          <p>this is add component</p>  

          <p>multiplication = {parseInt(props.a) * parseInt( props.b)}</p>
        </div>

    )


};

export default Add;